RSpec.describe Roma::Storage::Dbm do
  it "has a version number" do
    expect(Roma::Storage::Dbm::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
